import { useEffect, useRef, useState, } from 'react';
import ProfileManager from './ProfileManager';
import DriverLayoutEditor from './DriverLayoutEditor';
import { defaultDriverLayout, normalizeDriverLayout, } from './layoutTypes';
import { frostProfile, normalizeProfile, } from './profileTypes';
import './ControlPanel.css';
const SETTINGS_KEY='fuego-overlay-settings',LEGACY_SETTINGS_KEY='frost-overlay-settings',PROFILES_KEY='fuego-overlay-profiles',ACTIVE_PROFILE_KEY='fuego-overlay-active-profile',LAYOUT_PROFILES_KEY='fuego-driver-layout-profiles';
const defaultSettings={selectedCarNumber:'21',showDriverBar:true,showTicker:true,showTrackMap:true,showSponsorLogo:true,trackOverride:'auto',overlayLayout:'classic',driverLayout:structuredClone(defaultDriverLayout),rememberLastLayout:true,defaultOverlayLayout:'classic',showConditionalWidgetsInEditor:true,canvasPreset:'1080p',autoScaleOverlay:true,showSafeAreaGuides:false,speedUnit:'mph',fuelUnit:'liters',temperatureUnit:'fahrenheit',telemetryRate:'high',uiScale:1,overlayAnimations:true,animationSpeed:'normal',overlayOpacity:1};
const defaultOverlayStatus={driverConnections:0,tickerConnections:0},defaultRaceInformation={position:0,selectedLap:0,currentLap:0,totalLaps:0,gapToLeader:-1,onPitRoad:false,lastLapTime:0,bestLapTime:0};
const trackOptions=['Talladega Superspeedway','Texas Motor Speedway','Dover Motor Speedway','Charlotte Motor Speedway','Nashville Superspeedway','Michigan International Speedway','Pocono Raceway','Daytona International Speedway','Chicagoland Speedway','EchoPark Speedway','North Wilkesboro Speedway','Indianapolis Motor Speedway','Las Vegas Motor Speedway','Iowa Speedway','Richmond Raceway','New Hampshire Motor Speedway','Darlington Raceway','Bristol Motor Speedway','Kansas Speedway','Phoenix Raceway','Martinsville Speedway','Homestead-Miami Speedway'];
function loadSavedSettings(){try{const saved=localStorage.getItem(SETTINGS_KEY)||localStorage.getItem(LEGACY_SETTINGS_KEY);if(!saved)return defaultSettings;const parsed=JSON.parse(saved),merged={...defaultSettings,...parsed,driverLayout:normalizeDriverLayout(parsed.driverLayout)};if(!merged.rememberLastLayout)merged.overlayLayout=merged.defaultOverlayLayout;return merged}catch{return defaultSettings}}
function loadSavedProfiles(){try{const stored=localStorage.getItem(PROFILES_KEY);if(!stored)return[frostProfile];const parsed=JSON.parse(stored),custom=parsed.filter(p=>p.id!==frostProfile.id).map(p=>normalizeProfile(p)).map(p=>({...p,factory:false}));return[frostProfile,...custom]}catch{return[frostProfile]}}
function loadActiveProfileId(){return localStorage.getItem(ACTIVE_PROFILE_KEY)||frostProfile.id}
function formatLapTime(seconds){if(!Number.isFinite(seconds)||seconds<=0)return'--';const m=Math.floor(seconds/60),r=seconds-m*60;return`${m}:${r.toFixed(3).padStart(6,'0')}`}
function formatGap(position,gap){if(position===1)return'LEADER';if(!Number.isFinite(gap)||gap<0)return'--';return`+${gap.toFixed(3)}`}
function ControlPanel(){const socketRef=useRef(null),reconnectTimerRef=useRef(null),[activeTab,setActiveTab]=useState('driver'),[profiles,setProfiles]=useState(loadSavedProfiles),[activeProfileId,setActiveProfileId]=useState(loadActiveProfileId),[selectedProfileId,setSelectedProfileId]=useState(loadActiveProfileId),[editingProfileId,setEditingProfileId]=useState(loadActiveProfileId),[activeProfile,setActiveProfile]=useState(()=>profiles.find(p=>p.id===activeProfileId)||frostProfile),activeProfileRef=useRef(activeProfile);useEffect(()=>{activeProfileRef.current=activeProfile},[activeProfile]);const[serverConnected,setServerConnected]=useState(false),[iracingConnected,setIracingConnected]=useState(false),[trackName,setTrackName]=useState('Unknown Track'),[detectedTrackName,setDetectedTrackName]=useState('Unknown Track'),[driverName,setDriverName]=useState(''),[settings,setSettings]=useState(loadSavedSettings),[overlayStatus,setOverlayStatus]=useState(defaultOverlayStatus),[raceInformation,setRaceInformation]=useState(defaultRaceInformation),[copiedSource,setCopiedSource]=useState(null),driverOverlayUrl=`${window.location.origin}/`,tickerOverlayUrl=`${window.location.origin}/?view=ticker`;
useEffect(()=>{let stopped=false;function connect(){if(stopped)return;const socket=new WebSocket('ws://localhost:3200');socketRef.current=socket;socket.onopen=()=>{setServerConnected(true);socket.send(JSON.stringify({type:'registerClient',clientType:'control'}));socket.send(JSON.stringify({type:'updateSettings',settings:loadSavedSettings()}));socket.send(JSON.stringify({type:'updateProfile',profile:activeProfileRef.current}))};socket.onmessage=event=>{try{const message=JSON.parse(event.data);if(message.type==='profile')return;setIracingConnected(Boolean(message.connected));setTrackName(message.trackName||'Unknown Track');setDetectedTrackName(message.detectedTrackName||'Unknown Track');setDriverName(message.selectedDriverName||'');setRaceInformation({position:message.position??0,selectedLap:message.selectedLap??0,currentLap:message.currentLap??0,totalLaps:message.totalLaps??0,gapToLeader:message.gapToLeader??-1,onPitRoad:Boolean(message.onPitRoad),lastLapTime:message.lastLapTime??0,bestLapTime:message.bestLapTime??0});if(message.overlayStatus)setOverlayStatus(message.overlayStatus);if(message.settings){const received={...defaultSettings,...message.settings,driverLayout:normalizeDriverLayout(message.settings.driverLayout)};setSettings(received);localStorage.setItem(SETTINGS_KEY,JSON.stringify(received))}}catch(error){console.error('Could not read control-panel data:',error)}};socket.onerror=()=>setServerConnected(false);socket.onclose=()=>{setServerConnected(false);setIracingConnected(false);setOverlayStatus(defaultOverlayStatus);setRaceInformation(defaultRaceInformation);if(!stopped)reconnectTimerRef.current=window.setTimeout(connect,1500)}}connect();return()=>{stopped=true;if(reconnectTimerRef.current!==null)window.clearTimeout(reconnectTimerRef.current);socketRef.current?.close()}},[]);
function sendSettings(next){setSettings(next);localStorage.setItem(SETTINGS_KEY,JSON.stringify(next));const socket=socketRef.current;if(socket&&socket.readyState===WebSocket.OPEN)socket.send(JSON.stringify({type:'updateSettings',settings:next}))}
function sendProfile(profile){activeProfileRef.current=profile;setActiveProfile(profile);setActiveProfileId(profile.id);setSelectedProfileId(profile.id);localStorage.setItem(ACTIVE_PROFILE_KEY,profile.id);sendSettings({...settings,selectedCarNumber:profile.carNumber});const socket=socketRef.current;if(socket?.readyState===WebSocket.OPEN)socket.send(JSON.stringify({type:'updateProfile',profile}))}
function saveProfiles(next){setProfiles(next);localStorage.setItem(PROFILES_KEY,JSON.stringify(next))}function activateProfile(id){const p=profiles.find(x=>x.id===id);if(p)setEditingProfileId(p.id)}function saveProfile(profile){const normalized=normalizeProfile(profile),exists=profiles.some(x=>x.id===normalized.id),next=exists?profiles.map(x=>x.id===normalized.id?normalized:x):[...profiles,normalized];saveProfiles(next);setEditingProfileId(normalized.id)}function deleteProfile(id){if(id===frostProfile.id)return;const next=profiles.filter(p=>p.id!==id);saveProfiles(next);if(editingProfileId===id)setEditingProfileId(frostProfile.id);if(selectedProfileId===id)setSelectedProfileId(activeProfileId===id?frostProfile.id:activeProfileId);if(activeProfileId===id)sendProfile(frostProfile)}function applySelectedProfile(){const p=profiles.find(x=>x.id===selectedProfileId);if(p)sendProfile(p)}function selectTrack(trackOverride){sendSettings({...settings,trackOverride})}function selectOverlayLayout(overlayLayout){sendSettings({...settings,overlayLayout})}function updateDriverLayout(driverLayout){sendSettings({...settings,driverLayout})}function toggleSetting(settingName){sendSettings({...settings,[settingName]:!settings[settingName]})}function updatePreference(key,value){sendSettings({...settings,[key]:value})}function resetOverlaySettings(){sendSettings({...defaultSettings,selectedCarNumber:settings.selectedCarNumber,driverLayout:settings.driverLayout,overlayLayout:settings.overlayLayout})}function resetControlPanelSettings(){updatePreference('uiScale',1)}function resetSavedLayouts(){localStorage.removeItem(LAYOUT_PROFILES_KEY);window.dispatchEvent(new CustomEvent('fuego-layout-profiles-reset'))}function factoryResetFuego(){if(!window.confirm('Reset Fuego to factory defaults? Driver profiles, saved layouts, and preferences will be removed.'))return;[SETTINGS_KEY,LEGACY_SETTINGS_KEY,PROFILES_KEY,ACTIVE_PROFILE_KEY,LAYOUT_PROFILES_KEY].forEach(k=>localStorage.removeItem(k));setSettings(structuredClone(defaultSettings));window.location.reload()}function openDriverOverlay(){window.open(driverOverlayUrl,'_blank')}async function copySourceUrl(source){const url=source==='driver'?driverOverlayUrl:tickerOverlayUrl;try{await navigator.clipboard.writeText(url);setCopiedSource(source);window.setTimeout(()=>setCopiedSource(null),1500)}catch(error){console.error('Could not copy browser-source URL:',error)}}
const driverSourceConnected=overlayStatus.driverConnections>0,lapDisplay=raceInformation.totalLaps>0?`${raceInformation.selectedLap} / ${raceInformation.totalLaps}`:raceInformation.selectedLap>0?String(raceInformation.selectedLap):'--',controlProfileStyle={'--control-blue':activeProfile.primaryColor,'--control-deep-blue':activeProfile.secondaryColor,'--control-yellow':activeProfile.numberColor,'--control-white':activeProfile.textColor,'--control-trim':activeProfile.trimColor,'--control-ui-scale':settings.uiScale};
return <main className="control-stage" style={controlProfileStyle}><section className="control-panel"><div className="control-stripes"/><header className="control-header"><div className="control-number">{activeProfile.carNumber}</div><div><span className="control-kicker">{activeProfile.firstName}{' '}{activeProfile.nickname&&`“${activeProfile.nickname}” `}{activeProfile.lastName}</span><h1>OVERLAY CONTROL</h1></div><div className={['control-status',iracingConnected?'status-live':serverConnected?'status-waiting':'status-offline'].join(' ')}><span/>{iracingConnected?'iRACING LIVE':serverConnected?'WAITING FOR iRACING':'SERVER OFFLINE'}</div></header><nav className="control-tabs" aria-label="Control sections">{[['driver','DRIVER PROFILE'],['overlay','OVERLAY CONTROL'],['broadcaster','BROADCASTER'],['race','RACE CONTROL'],['settings','SETTINGS']].map(([tab,label])=><button key={tab} type="button" className={activeTab===tab?'active':''} onClick={()=>setActiveTab(tab)}>{label}</button>)}</nav>{activeTab==='driver'&&<div className="tab-page"><section className="control-card driver-card driver-profile-summary"><h2>ACTIVE DRIVER PROFILE</h2><label className="control-label" htmlFor="selected-profile">DRIVER PROFILE</label><div className="profile-select-row"><select id="selected-profile" value={selectedProfileId} onChange={e=>setSelectedProfileId(e.target.value)}>{profiles.map(p=><option key={p.id} value={p.id}>{`#${p.carNumber} — ${p.profileName}`}</option>)}</select><button type="button" className="apply-button" onClick={applySelectedProfile}>APPLY PROFILE</button></div><div className="selected-driver"><strong>#{activeProfile.carNumber}</strong><span>{activeProfile.firstName}{' '}{activeProfile.nickname&&`“${activeProfile.nickname}” `}{activeProfile.lastName}</span></div><div className="detected-driver">iRACING: {driverName||'DRIVER NOT DETECTED'}</div></section><ProfileManager profiles={profiles} activeProfileId={editingProfileId} onActivate={activateProfile} onSave={saveProfile} onDelete={deleteProfile}/></div>}{activeTab==='overlay'&&<div className="control-grid"><section className="control-card layout-selector-card"><h2>ONBOARD OVERLAY LAYOUT</h2><div className="layout-selector-buttons"><button type="button" className={settings.overlayLayout==='classic'?'layout-choice active':'layout-choice'} onClick={()=>selectOverlayLayout('classic')}><strong>FUEGO CLASSIC</strong><span>Current overlay</span></button><button type="button" className={settings.overlayLayout==='driver'?'layout-choice active':'layout-choice'} onClick={()=>selectOverlayLayout('driver')}><strong>FUEGO DRIVER</strong><span>Modular driver-feed layout</span></button><button type="button" className="layout-choice disabled" disabled><strong>FUEGO COCKPIT</strong><span>Coming next</span></button></div></section>{settings.overlayLayout==='driver'&&<DriverLayoutEditor value={settings.driverLayout} onChange={updateDriverLayout} showConditionalWidgets={settings.showConditionalWidgetsInEditor} showSafeAreaGuides={settings.showSafeAreaGuides}/>}<section className="control-card race-card"><h2>RACE INFORMATION</h2><dl className="race-info-list"><div><dt>POSITION</dt><dd>{raceInformation.position>0?`P${raceInformation.position}`:'--'}</dd></div><div><dt>LAP</dt><dd>{lapDisplay}</dd></div><div><dt>GAP</dt><dd>{formatGap(raceInformation.position,raceInformation.gapToLeader)}</dd></div><div><dt>STATUS</dt><dd className={raceInformation.onPitRoad?'race-status-pit':'race-status-track'}>{iracingConnected?(raceInformation.onPitRoad?'PIT ROAD':'ON TRACK'):'--'}</dd></div><div><dt>LAST LAP</dt><dd>{formatLapTime(raceInformation.lastLapTime)}</dd></div><div><dt>BEST LAP</dt><dd>{formatLapTime(raceInformation.bestLapTime)}</dd></div></dl></section><section className="control-card session-card"><h2>SESSION STATUS</h2><dl className="status-list"><div><dt>TELEMETRY</dt><dd>{iracingConnected?'CONNECTED':'DISCONNECTED'}</dd></div><div><dt>DETECTED TRACK</dt><dd>{detectedTrackName}</dd></div><div><dt>MAP IN USE</dt><dd>{trackName}</dd></div></dl><label className="control-label track-select-label" htmlFor="track-override">TRACK MAP OVERRIDE</label><select id="track-override" className="track-select" value={settings.trackOverride} onChange={e=>selectTrack(e.target.value)}><option value="auto">AUTOMATIC — USE iRACING</option>{trackOptions.map(track=><option key={track} value={track}>{track.toUpperCase()}</option>)}</select></section><section className="control-card elements-card"><h2>OVERLAY ELEMENTS</h2>{[['showDriverBar','DRIVER INFORMATION'],['showTicker','RACE TICKER'],['showTrackMap','TRACK MAP'],['showSponsorLogo','PRIMARY SPONSOR LOGO']].map(([key,label])=><button key={key} type="button" className="toggle-row" onClick={()=>toggleSetting(key)}><span>{label}</span><span className={settings[key]?'toggle-switch toggle-on':'toggle-switch'}><i/></span></button>)}</section><section className="control-card obs-card"><h2>OBS BROWSER SOURCE</h2><p className="control-help">Add this source to OBS once. Fuego Classic, Fuego Driver, and future onboard layouts all render through this single source.</p><div className="obs-source"><div className="obs-source-heading"><span className={driverSourceConnected?'obs-indicator obs-connected':'obs-indicator'}/><strong>FUEGO OVERLAY</strong><em>{driverSourceConnected?`CONNECTED (${overlayStatus.driverConnections})`:'NOT CONNECTED'}</em></div><div className="obs-url-row"><input value={driverOverlayUrl} readOnly/><button type="button" onClick={()=>copySourceUrl('driver')}>{copiedSource==='driver'?'COPIED':'COPY'}</button></div></div><div className="source-buttons compact-source-buttons"><button type="button" onClick={openDriverOverlay}>OPEN FUEGO OVERLAY</button></div></section></div>}{activeTab==='broadcaster'&&<section className="coming-soon-page"><span className="coming-soon-kicker">FUEGO BROADCAST SUITE</span><h2>BROADCASTER</h2><p>Camera switching, driver focus, live TV graphics, replay controls, and broadcast hotkeys are planned for a future release.</p><strong>COMING SOON</strong></section>}{activeTab==='race'&&<section className="coming-soon-page"><span className="coming-soon-kicker">FUEGO RACE ADMINISTRATION</span><h2>RACE CONTROL</h2><p>Caution controls, pace-lap management, driver black flags, DQ protection, command lockouts, and race-control history are planned for a future release.</p><strong>COMING SOON</strong></section>}{activeTab==='settings'&&<div className="settings-page"><section className="control-card settings-card"><h2>APPLICATION</h2><dl className="settings-list"><div><dt>VERSION</dt><dd>v1.1.0</dd></div><div><dt>UPDATE CHANNEL</dt><dd>STABLE</dd></div><div><dt>AUTOMATIC UPDATE CHECK</dt><dd className="setting-enabled">ENABLED</dd></div><div><dt>STARTUP SPLASH</dt><dd className="setting-enabled">ENABLED</dd></div></dl><a className="settings-action" href="https://github.com/frosstbiite/fuego-overlay/releases/latest" target="_blank" rel="noreferrer">CHECK FOR UPDATES</a></section><section className="control-card settings-card"><h2>DRIVER LAYOUT</h2><div className="settings-form-grid"><label className="settings-toggle-row"><span><strong>REMEMBER LAST LAYOUT</strong><small>Open the layout that was active when Fuego closed.</small></span><input type="checkbox" checked={settings.rememberLastLayout} onChange={e=>updatePreference('rememberLastLayout',e.target.checked)}/></label><label><span>DEFAULT STARTUP LAYOUT</span><select value={settings.defaultOverlayLayout} onChange={e=>updatePreference('defaultOverlayLayout',e.target.value)} disabled={settings.rememberLastLayout}><option value="classic">Fuego Classic</option><option value="driver">Fuego Driver</option></select></label><label className="settings-toggle-row"><span><strong>SHOW CONDITIONAL WIDGETS IN EDITOR</strong><small>Keep conditional widgets visible while designing a layout.</small></span><input type="checkbox" checked={settings.showConditionalWidgetsInEditor} onChange={e=>updatePreference('showConditionalWidgetsInEditor',e.target.checked)}/></label></div></section><section className="control-card settings-card"><h2>OBS / DISPLAY</h2><div className="settings-form-grid"><label><span>BASE CANVAS</span><select value={settings.canvasPreset} onChange={e=>updatePreference('canvasPreset',e.target.value)}><option value="720p">1280 × 720</option><option value="1080p">1920 × 1080</option><option value="1440p">2560 × 1440</option><option value="4k">3840 × 2160</option></select></label><label className="settings-toggle-row"><span><strong>AUTO-SCALE OVERLAY</strong><small>Scale percentage-based Driver layouts with the OBS browser canvas.</small></span><input type="checkbox" checked={settings.autoScaleOverlay} onChange={e=>updatePreference('autoScaleOverlay',e.target.checked)}/></label><label className="settings-toggle-row"><span><strong>SAFE-AREA GUIDES</strong><small>Show broadcast-safe guides inside the Driver layout editor.</small></span><input type="checkbox" checked={settings.showSafeAreaGuides} onChange={e=>updatePreference('showSafeAreaGuides',e.target.checked)}/></label></div></section><section className="control-card settings-card"><h2>TELEMETRY</h2><div className="settings-form-grid settings-three-column"><label><span>SPEED</span><select value={settings.speedUnit} onChange={e=>updatePreference('speedUnit',e.target.value)}><option value="mph">MPH</option><option value="kph">KPH</option></select></label><label><span>FUEL</span><select value={settings.fuelUnit} onChange={e=>updatePreference('fuelUnit',e.target.value)}><option value="liters">Liters</option><option value="gallons">Gallons</option><option value="percent">Percent</option></select></label><label><span>TEMPERATURE</span><select value={settings.temperatureUnit} onChange={e=>updatePreference('temperatureUnit',e.target.value)}><option value="fahrenheit">°F</option><option value="celsius">°C</option></select></label><label><span>UPDATE RATE</span><select value={settings.telemetryRate} onChange={e=>updatePreference('telemetryRate',e.target.value)}><option value="normal">Normal · 30 Hz</option><option value="high">High · 60 Hz</option></select></label></div></section><section className="control-card settings-card"><h2>APPEARANCE</h2><div className="settings-form-grid"><label><span>CONTROL PANEL SCALE · {Math.round(settings.uiScale*100)}%</span><input type="range" min="0.85" max="1.2" step="0.05" value={settings.uiScale} onChange={e=>updatePreference('uiScale',Number(e.target.value))}/></label><label className="settings-toggle-row"><span><strong>OVERLAY ANIMATIONS</strong><small>Enable flag sweeps and other overlay transitions.</small></span><input type="checkbox" checked={settings.overlayAnimations} onChange={e=>updatePreference('overlayAnimations',e.target.checked)}/></label><label><span>ANIMATION SPEED</span><select value={settings.animationSpeed} disabled={!settings.overlayAnimations} onChange={e=>updatePreference('animationSpeed',e.target.value)}><option value="slow">Slow</option><option value="normal">Normal</option><option value="fast">Fast</option></select></label><label><span>OVERLAY OPACITY · {Math.round(settings.overlayOpacity*100)}%</span><input type="range" min="0.4" max="1" step="0.05" value={settings.overlayOpacity} onChange={e=>updatePreference('overlayOpacity',Number(e.target.value))}/></label></div></section><section className="control-card settings-card"><h2>LOCAL SERVICES</h2><dl className="settings-list"><div><dt>OVERLAY SERVER</dt><dd>127.0.0.1:5173</dd></div><div><dt>TELEMETRY SERVICE</dt><dd>127.0.0.1:3200</dd></div><div><dt>TELEMETRY STATUS</dt><dd>{serverConnected?'ONLINE':'OFFLINE'}</dd></div><div><dt>iRACING STATUS</dt><dd>{iracingConnected?'CONNECTED':'WAITING'}</dd></div></dl></section><section className="control-card settings-card settings-recovery"><h2>DEFAULTS / RECOVERY</h2><p>Reset only the part of Fuego you need. Factory Reset removes local driver profiles, saved layouts, and application preferences.</p><div className="settings-recovery-actions"><button type="button" onClick={resetOverlaySettings}>RESET OVERLAY SETTINGS</button><button type="button" onClick={resetControlPanelSettings}>RESET CONTROL PANEL</button><button type="button" onClick={resetSavedLayouts}>RESET SAVED LAYOUTS</button><button type="button" className="settings-danger" onClick={factoryResetFuego}>FACTORY RESET FUEGO</button></div></section></div>}<footer className="control-footer">FUEGO OVERLAY v1.1.0{' • '}© 2026 FUEGO AUTOSPORT{' • '}ALL RIGHTS RESERVED</footer></section></main>}
export default ControlPanel;
